return {
	-- This isn't compatible with lsp in nvim 0.11
	-- I don't know if they will update it so for now ts_ls is in use in lsp.lua
	--
	-- 	"pmizio/typescript-tools.nvim",
	-- 	dependencies = { "nvim-lua/plenary.nvim", "neovim/nvim-lspconfig" },
	-- 	opts = {},
	-- 	config = function()
	-- 		require("typescript-tools").setup({})
	-- 	end,
}
